import React from "react";

import "./Map.css";

const Map = (props) => {
  return (
    <div className={`map ${props.className}`} style={`props.style`}>
      This is maps for google but unfortunately it is not working.+ because use
      google maps api need creditcard and i dont have one. so i use dummy one
      using iframe from udemyforum
    </div>
  );
};

export default Map;
